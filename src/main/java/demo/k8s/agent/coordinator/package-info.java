/**
 * Coordinator Mode 第一阶段：主会话工具分区（Task + SendMessage + TaskStop 占位）与内存邮箱。
 * 对齐 Zread {@code Coordinator Mode} 中「协调者与工作者不对称工具集」的<strong>结构</strong>，非 Claude Code 全量实现。
 */
package demo.k8s.agent.coordinator;
